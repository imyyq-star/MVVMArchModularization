android {
    defaultConfig {
        applicationId "com.imyyq.modularization.module1"
    }
}

dependencies {
    implementation fileTree(dir: "libs", include: ["*.jar"])
    implementation project(path: ':module1_lib')
}